<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.sistec_event">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".PostList"></activity>
        <activity android:name=".Register" />
        <activity android:name=".View_All_Users" />
        <activity android:name=".Add_Event" />
        <activity android:name=".Admin_Home_Acivity" />
        <activity android:name=".List_of_Event" />
        <activity android:name=".Admin_Login" />
        <activity android:name=".Home_Activity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>